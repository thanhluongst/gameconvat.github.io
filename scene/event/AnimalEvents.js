window.AnimalEvents = {
    onAnimalClick: function(scene, animalKey) {
        // Xử lý sự kiện click động vật
        console.log(`Animal clicked: ${animalKey} in scene: ${scene}`);
        // Thêm logic xử lý sự kiện tại đây
    }
    // Bổ sung các event khác nếu cần
};